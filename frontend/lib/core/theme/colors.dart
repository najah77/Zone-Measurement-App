import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const Color primary = Color(0xFF006B8F); // Teal/Blue family
  static const Color primaryLight = Color(0xFFB3EBF2);
  static const Color secondary = Color(0xFF455A64);

  // Backgrounds
  static const Color background = Color(0xFFF5F7FA);
  static const Color surface = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF1D1B20);
  static const Color textSecondary = Color(0xFF49454F);

  // States
  static const Color success = Color(0xFF4CAF50); // Sensitive (Green)
  static const Color warning = Color(0xFFFF9800); // Intermediate (Orange)
  static const Color error = Color(0xFFF44336); // Resistant (Red)
  static const Color info = Color(0xFF2196F3);

  // UI Elements
  static const Color border = Color(0xFFE0E0E0);
  static const Color divider = Color(0xFFEEEEEE);
}
