class AppConstants {
  // App Info
  static const String appName = 'AST Analyzer';
  static const String appTagline = 'Upload plates. Get results.';

  // Button Texts
  static const String captureImage = 'Capture Image';
  static const String uploadGallery = 'Upload from Gallery';
  static const String analyze = 'Proceed to Analysis';
  static const String viewResults = 'View Results';
  static const String selectImage = 'Select Image';

  // Analysis
  static const String detectedAntibiotics = 'Detected Antibiotics';
  static const String zoneDiameter = 'Zone Diameter (mm)';
  static const String interpretation = 'Interpretation';

  // Assets (Using placeholders if actual assets effectively don't exist yet, usually in assets/images)
  // Ensure to add these to pubspec.yaml if we add real files.
  // For Phase 0, we heavily rely on code-generated UI or SVGs if needed, but strings are here.

  // Layout
  static const double defaultPadding = 16.0;
  static const double smallPadding = 8.0;
  static const double cardBorderRadius = 12.0;
}
